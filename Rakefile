require 'bundler'
require 'pathname'
require 'logger'
require 'fileutils'
require 'rake/clean'
Bundler.require

CLEAN.include FileList['public/**/*.js', 'public/**/*.html']

desc 'Compile sources into public for deploy'
task :build do
	
end

task :default => :build
